coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        informational: true
comment:
  layout: "diff, files"
ignore:
  - "tools/**"
  - "tests/**"
